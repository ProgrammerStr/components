(function (app) {
    'use strict';

    /*
     * DEBUG PARA O CONSOLE
    **/
    app.Debug(true);
    /*
     * INICIALIZANDO MODULOS
    **/
    app.Init({
        modules : app.Modules
    });
}(APP));
